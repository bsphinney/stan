# STAN Community Benchmark Configuration
# ───────────────────────────────────────
# Community submission is opt-in. Set community_submit: true on each
# instrument in instruments.yml to participate.
#
# No HuggingFace account or token is needed. STAN submits through a
# relay API that handles authentication automatically.

display_name: ""
hela_source: "Pierce HeLa Protein Digest Standard"
institution_type: "core_facility"
